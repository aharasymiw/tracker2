# Migrations

Drizzle-generated SQL migrations live here. Generate with
`pnpm --filter @lod/server db:generate`. Review every file in a dedicated PR.
Never edit an already-applied migration — write a new one.
